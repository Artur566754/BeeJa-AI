# BeeJa AI Model

Собственная ИИ модель с возможностью обучения на пользовательских датасетах, автоматическим дообучением и чат-интерфейсом. Поддерживает как LSTM, так и современную Transformer архитектуру.

## Возможности

- 🧠 **Две архитектуры моделей**:
  - **LSTM** - классическая рекуррентная сеть (character-level)
  - **Transformer** - современная архитектура с self-attention (BPE tokenization)
- 📚 **Мультиформатная загрузка данных** - поддержка .txt, .pdf, .docx файлов
- 🔄 **Автоматическое дообучение** - модель обучается при добавлении новых файлов
- 💬 **Чат-интерфейс** - общайтесь с моделью через командную строку
- 🎯 **Контекстные ответы** - Transformer поддерживает историю разговора
- 💾 **Сохранение весов** - модель сохраняет свои знания между сессиями
- 🛡️ **Защита от ошибок** - автоматический откат при неудачном обучении
- ⚡ **Оптимизация производительности** - поддержка GPU и mixed precision training

## Установка

### Требования

- Python 3.8+
- pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

## Использование

### Выбор модели

Система поддерживает два типа моделей:

1. **LSTM** (по умолчанию) - быстрая, простая, character-level
2. **Transformer** - современная, более качественная, с BPE токенизацией

### 1. Обучение модели

#### LSTM модель (по умолчанию)

```bash
python main.py --train
```

#### Transformer модель

```bash
python main.py --train --model-type transformer --model-size small
```

Размеры Transformer модели:
- `small` - быстрое обучение, 4 слоя, 128 dim (рекомендуется для начала)
- `medium` - баланс качества и скорости, 6 слоев, 256 dim
- `large` - лучшее качество, 12 слоев, 512 dim (требует GPU)

Параметры обучения:
- `--epochs` - количество эпох (по умолчанию: 50)
- `--lr` - скорость обучения (по умолчанию: 0.001 для LSTM, 0.0001 для Transformer)
- `--batch-size` - размер батча (по умолчанию: 32 для LSTM, 16 для Transformer)
- `--mixed-precision` - использовать mixed precision training (только GPU)

Примеры:

```bash
# LSTM с кастомными параметрами
python main.py --train --model-type lstm --epochs 100 --lr 0.0005 --batch-size 16

# Transformer small (быстро)
python main.py --train --model-type transformer --model-size small --epochs 30

# Transformer medium с GPU оптимизацией
python main.py --train --model-type transformer --model-size medium --epochs 50 --mixed-precision

# Transformer large (требует много памяти)
python main.py --train --model-type transformer --model-size large --epochs 100 --batch-size 8
```

### 2. Чат с моделью

#### LSTM модель

```bash
python main.py --chat --model-type lstm
```

или просто:

```bash
python main.py
```

#### Transformer модель

```bash
python main.py --chat --model-type transformer --model-size small
```

Transformer автоматически использует контекст разговора для более релевантных ответов.

Команды в чате:
- `/exit` - выход из чата
- `/retrain` - информация о переобучении
- `/clear` - очистить историю разговора

### 3. Автоматическое обучение

Включите режим автоматического обучения:

```bash
python main.py --auto
```

Модель будет автоматически переобучаться при добавлении новых файлов в папку `datasets/`.

## Структура проекта

```
.
├── src/                    # Исходный код
│   ├── model.py           # LSTM модель
│   ├── transformer_model.py  # Transformer модель
│   ├── transformer_components.py  # Компоненты Transformer
│   ├── tokenizer.py       # BPE токенизатор
│   ├── text_generator.py  # Стратегии генерации текста
│   ├── conversation_context.py  # Управление контекстом
│   ├── evaluation.py      # Инструменты оценки моделей
│   ├── dataset_loader.py  # Загрузчик датасетов
│   ├── training_pipeline.py  # Пайплайн обучения
│   ├── chat_interface.py  # Чат-интерфейс
│   ├── vocabulary.py      # Словарь символов (LSTM)
│   └── config.py          # Конфигурация
├── datasets/              # Датасеты для обучения
│   └── initial_dataset.txt  # Начальный датасет
├── models/                # Сохраненные модели
│   ├── ai_model.pth      # LSTM checkpoint
│   ├── transformer_small.pth   # Transformer small
│   ├── transformer_medium.pth  # Transformer medium
│   └── transformer_large.pth   # Transformer large
├── tests/                 # Тесты
│   ├── unit/             # Unit тесты
│   ├── property/         # Property-based тесты
│   └── integration/      # Интеграционные тесты
├── main.py               # Главный файл приложения
└── requirements.txt      # Зависимости
```

## Добавление датасетов

1. Поместите файлы в папку `datasets/`
2. Поддерживаемые форматы:
   - `.txt` - текстовые файлы
   - `.pdf` - PDF документы
   - `.docx` - Word документы
3. Запустите обучение: `python main.py --train`

## Начальный датасет

Модель инициализирована с информацией о создателях:
- **Создатель**: Jamsaide (Артур)
- **Помощники**: BeeBoo (Никита) и Araxium (Лев)

## Архитектура моделей

### LSTM модель

- **Тип**: Character-level LSTM
- **Embedding размерность**: 128
- **Hidden размерность**: 256
- **Количество слоев**: 2
- **Токенизация**: Character-level
- **Оптимизатор**: Adam
- **Loss функция**: CrossEntropyLoss

### Transformer модель

#### Small (рекомендуется для начала)
- **Embedding размерность**: 128
- **Количество голов**: 4
- **Количество слоев**: 4
- **FFN размерность**: 512
- **Максимальная длина**: 256 токенов
- **Токенизация**: BPE (vocab_size=5000)

#### Medium (баланс)
- **Embedding размерность**: 256
- **Количество голов**: 8
- **Количество слоев**: 6
- **FFN размерность**: 1024
- **Максимальная длина**: 512 токенов
- **Токенизация**: BPE (vocab_size=5000)

#### Large (лучшее качество)
- **Embedding размерность**: 512
- **Количество голов**: 16
- **Количество слоев**: 12
- **FFN размерность**: 2048
- **Максимальная длина**: 1024 токенов
- **Токенизация**: BPE (vocab_size=5000)

### Особенности Transformer

- **Self-Attention**: Модель учитывает все предыдущие токены
- **Positional Encoding**: Сохраняет информацию о позиции токенов
- **Causal Masking**: Предотвращает "подглядывание" в будущее
- **Layer Normalization**: Стабилизирует обучение
- **Residual Connections**: Улучшает градиентный поток
- **Memory-Efficient Attention**: Для длинных последовательностей (>512 токенов)

### Стратегии генерации текста

Transformer поддерживает несколько стратегий генерации:

1. **Greedy Decoding** - выбор наиболее вероятного токена
2. **Top-K Sampling** - выбор из K наиболее вероятных токенов
3. **Nucleus (Top-P) Sampling** - выбор из токенов с суммарной вероятностью P
4. **Temperature Scaling** - контроль случайности (выше = более случайно)
5. **Repetition Penalty** - уменьшение повторений

## Тестирование

Запуск всех тестов:

```bash
pytest tests/ -v
```

Запуск unit тестов:

```bash
pytest tests/test_*.py -v
```

Запуск property-based тестов:

```bash
pytest tests/test_*_properties.py -v
```

## Примеры использования

### Обучение на своих данных

#### LSTM

1. Создайте файл `datasets/my_data.txt` с вашим текстом
2. Запустите обучение:
   ```bash
   python main.py --train --model-type lstm --epochs 100
   ```
3. Начните чат:
   ```bash
   python main.py --model-type lstm
   ```

#### Transformer

1. Создайте файл `datasets/my_data.txt` с вашим текстом (рекомендуется >10KB)
2. Запустите обучение:
   ```bash
   python main.py --train --model-type transformer --model-size small --epochs 50
   ```
3. Начните чат:
   ```bash
   python main.py --model-type transformer --model-size small
   ```

### Дообучение модели

Обе модели поддерживают инкрементальное обучение:

1. Добавьте новые файлы в `datasets/`
2. Запустите обучение снова:
   ```bash
   # LSTM
   python main.py --train --model-type lstm --epochs 30 --lr 0.0001
   
   # Transformer
   python main.py --train --model-type transformer --model-size small --epochs 20 --lr 0.00005
   ```

Используйте меньший learning rate для дообучения, чтобы не забыть предыдущие знания.

### Сравнение моделей

Вы можете обучить обе модели и сравнить их:

```bash
# Обучить LSTM
python main.py --train --model-type lstm --epochs 50

# Обучить Transformer
python main.py --train --model-type transformer --model-size small --epochs 50

# Чат с LSTM
python main.py --model-type lstm

# Чат с Transformer
python main.py --model-type transformer --model-size small
```

### Оптимизация производительности

#### GPU ускорение

Transformer автоматически использует GPU если доступен:

```bash
# Mixed precision training (быстрее на GPU)
python main.py --train --model-type transformer --model-size medium --mixed-precision
```

#### CPU режим

Для CPU рекомендуется использовать small модель:

```bash
python main.py --train --model-type transformer --model-size small --batch-size 8
```

## Устранение неполадок

### Модель не обучается

- Убедитесь, что в `datasets/` есть файлы
- Проверьте, что файлы содержат достаточно текста:
  - LSTM: минимум 100 символов
  - Transformer: рекомендуется >10KB для хорошего качества
- Попробуйте увеличить количество эпох

### Ошибка при загрузке PDF

- Установите PyPDF2: `pip install PyPDF2`
- Убедитесь, что PDF не поврежден

### Модель генерирует странный текст

#### LSTM
- Обучите модель дольше (больше эпох)
- Добавьте больше данных для обучения
- Попробуйте изменить temperature при генерации

#### Transformer
- Убедитесь, что обучили достаточно эпох (минимум 30-50)
- Добавьте больше разнообразных данных
- Попробуйте увеличить model-size (small → medium)
- Настройте параметры генерации (temperature, top_k, top_p)

### Out of Memory (OOM)

Если не хватает памяти при обучении Transformer:

1. Уменьшите batch-size:
   ```bash
   python main.py --train --model-type transformer --batch-size 4
   ```

2. Используйте меньшую модель:
   ```bash
   python main.py --train --model-type transformer --model-size small
   ```

3. Для CPU используйте LSTM:
   ```bash
   python main.py --train --model-type lstm
   ```

### Медленное обучение

- Используйте GPU если доступен
- Включите mixed precision: `--mixed-precision`
- Уменьшите model-size или batch-size
- Для быстрых экспериментов используйте LSTM

## Производительность

### Сравнение LSTM vs Transformer

| Метрика | LSTM | Transformer Small | Transformer Medium |
|---------|------|-------------------|-------------------|
| Скорость обучения | Быстро | Средне | Медленно |
| Качество генерации | Базовое | Хорошее | Отличное |
| Память | ~10MB | ~50MB | ~200MB |
| Контекст | Ограничен | 256 токенов | 512 токенов |
| Токенизация | Character | BPE | BPE |

### Рекомендации

- **Для быстрых экспериментов**: LSTM
- **Для качественной генерации**: Transformer Small/Medium
- **Для production**: Transformer Medium с GPU
- **Для CPU**: LSTM или Transformer Small с малым batch-size

## Лицензия

Этот проект создан для образовательных целей.

## Авторы

- **Создатель**: Jamsaide (Артур)
- **Помощники**: BeeBoo (Никита), Araxium (Лев)
