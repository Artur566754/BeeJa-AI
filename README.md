# BeeJa AI Model 🤖

Собственная ИИ модель с возможностью обучения на пользовательских датасетах, автоматическим дообучением и чат-интерфейсом. Поддерживает как LSTM, так и современную Transformer архитектуру (аналог GPT).

## Возможности

- 🧠 **Две архитектуры моделей**:
  - **LSTM** - классическая рекуррентная сеть (посимвольная токенизация)
  - **Transformer** - современная архитектура с multi-head attention (BPE токенизация, как в GPT)
- 📚 **Мультиформатная загрузка данных** - поддержка .txt, .pdf, .docx файлов
- 🔄 **Автоматическое дообучение** - модель обучается при добавлении новых файлов
- 💬 **Чат-интерфейс** - общайтесь с моделью через командную строку
- 🎯 **Контекстные ответы** - Transformer запоминает историю разговора (до 5 сообщений)
- 💾 **Сохранение весов** - модель сохраняет свои знания между сессиями
- 🛡️ **Защита от ошибок** - автоматический откат при неудачном обучении
- ⚡ **Оптимизация производительности** - поддержка GPU и mixed precision training
- 📊 **Прогресс-бары** - визуализация процесса обучения с tqdm

## Что нового в Transformer архитектуре?

### Раньше (LSTM):
- Посимвольная обработка текста (каждая буква = токен)
- Короткая память, забывает длинные контексты
- Простая генерация текста
- Словарь ~100-200 символов

### Сейчас (Transformer):
- **BPE токенизация** - умные подслова (5000 токенов вместо символов)
  - Пример: "привет" = 1-2 токена вместо 6 букв
  - Модель учится быстрее и понимает морфологию слов
- **Multi-Head Attention** - модель "смотрит" на текст с 4-16 разных точек зрения одновременно
- **Контекст до 256-1024 токенов** - понимает длинные зависимости
- **Управление историей диалога** - запоминает предыдущие сообщения
- **Продвинутая генерация** - greedy, top-k, nucleus sampling, temperature control

## Установка

### Требования

- Python 3.8+
- pip

### Установка зависимостей

```bash
pip install -r requirements.txt
```

## Использование

### Выбор модели

Система поддерживает два типа моделей:

1. **LSTM** - быстрая, простая, посимвольная (для экспериментов)
2. **Transformer** - современная, качественная, с BPE токенизацией (рекомендуется)

### 1. Обучение модели

#### Быстрый старт (Transformer Small)

```bash
# Обучение на всех датасетах из папки datasets/
python main.py --train --model-type transformer --model-size small --epochs 50 --batch-size 64 --lr 0.0005
```

#### LSTM модель (для сравнения)

```bash
python main.py --train --model-type lstm --epochs 100 --lr 0.001
```

#### Размеры Transformer модели

- `small` - **рекомендуется для начала**
  - 4 слоя, 128 dim, 4 головы внимания
  - Быстрое обучение, хорошее качество
  - Подходит для CPU и GPU
  
- `medium` - баланс качества и скорости
  - 6 слоев, 256 dim, 8 голов внимания
  - Лучшее качество, требует больше времени
  - Рекомендуется GPU
  
- `large` - максимальное качество
  - 12 слоев, 512 dim, 16 голов внимания
  - Лучшее качество, долгое обучение
  - Требует GPU с большой памятью

#### Параметры обучения

- `--epochs` - количество эпох (рекомендуется: 50-500 для Transformer, 100+ для LSTM)
- `--lr` - скорость обучения:
  - `0.0005` - медленно, но качественно (рекомендуется для Transformer)
  - `0.0001` - очень медленно, для дообучения
  - `0.001` - стандартно для LSTM
- `--batch-size` - размер батча:
  - `64` - рекомендуется для Transformer на GPU
  - `32` - для CPU или малой памяти
  - `8-16` - для large модели
- `--mixed-precision` - ускорение на GPU (только для CUDA)

#### Примеры команд

```bash
# Качественное обучение Transformer (рекомендуется)
python main.py --train --model-type transformer --model-size small --epochs 500 --batch-size 64 --lr 0.0005

# Быстрое обучение для тестов
python main.py --train --model-type transformer --model-size small --epochs 30 --batch-size 32 --lr 0.001

# Transformer medium с GPU оптимизацией
python main.py --train --model-type transformer --model-size medium --epochs 100 --batch-size 32 --lr 0.0003 --mixed-precision

# Transformer large (требует мощный GPU)
python main.py --train --model-type transformer --model-size large --epochs 200 --batch-size 16 --lr 0.0001 --mixed-precision

# LSTM для сравнения
python main.py --train --model-type lstm --epochs 100 --lr 0.001 --batch-size 32
```

#### Что происходит во время обучения?

1. **Загрузка датасетов** - все .txt файлы из папки `datasets/`
2. **Построение BPE токенизатора** - создание словаря из 5000 токенов (занимает 2-5 минут)
3. **Подготовка данных** - создание последовательностей для обучения
4. **Обучение с прогресс-барами**:
   ```
   ============================================================
   Starting training: 500 epochs, 45231 sequences
   Batch size: 64, Learning rate: 0.0005
   ============================================================
   
   Training Progress:  15%|████▌         | 75/500 [12:30<1:11:20, 10.07s/epoch]
   Epoch 75/500: 100%|██████████| 707/707 [00:10<00:00, 68.5batch/s] loss: 2.1234, avg_loss: 2.1456
   ```
   
   Вы увидите:
   - Общий прогресс по эпохам
   - Прогресс батчей внутри каждой эпохи
   - Loss (потери) - должны уменьшаться
   - Perplexity (сложность) - должна уменьшаться
   - Время на эпоху и оценку оставшегося времени

### 2. Чат с моделью

#### Transformer модель (рекомендуется)

```bash
python main.py --chat --model-type transformer --model-size small
```

или короче:

```bash
python main.py --model-type transformer --model-size small
```

#### LSTM модель

```bash
python main.py --chat --model-type lstm
```

или просто:

```bash
python main.py
```

#### Особенности чата с Transformer

- Запоминает последние 5 сообщений
- Генерирует более связные и осмысленные ответы
- Понимает контекст разговора
- Использует nucleus sampling для разнообразия ответов

#### Команды в чате

- `/exit` - выход из чата
- `/retrain` - информация о переобучении
- `/clear` - очистить историю разговора (только Transformer)

### 3. Автоматическое обучение

Включите режим автоматического обучения:

```bash
python main.py --auto --model-type transformer --model-size small
```

Модель будет автоматически переобучаться при добавлении новых файлов в папку `datasets/`.

## Структура проекта

```
.
├── src/                    # Исходный код
│   ├── model.py           # LSTM модель
│   ├── transformer_model.py  # Transformer модель
│   ├── transformer_components.py  # Multi-head attention, FFN, etc.
│   ├── tokenizer.py       # BPE токенизатор (5000 токенов)
│   ├── text_generator.py  # Стратегии генерации (greedy, top-k, nucleus)
│   ├── conversation_context.py  # Управление историей диалога
│   ├── evaluation.py      # Инструменты оценки моделей
│   ├── dataset_loader.py  # Загрузчик датасетов
│   ├── training_pipeline.py  # Пайплайн обучения с прогресс-барами
│   ├── chat_interface.py  # Чат-интерфейс
│   ├── vocabulary.py      # Словарь символов (LSTM)
│   └── config.py          # Конфигурация
├── datasets/              # Датасеты для обучения
│   ├── all_combined.txt  # Объединенный датасет
│   └── *.txt             # Ваши датасеты
├── models/                # Сохраненные модели
│   ├── ai_model.pth      # LSTM checkpoint
│   ├── transformer_small.pth   # Transformer small
│   ├── transformer_medium.pth  # Transformer medium
│   └── transformer_large.pth   # Transformer large
├── tests/                 # Тесты (54+ property-based тестов)
│   ├── unit/             # Unit тесты
│   ├── property/         # Property-based тесты (Hypothesis)
│   └── integration/      # Интеграционные тесты
├── telegram_bot/          # Telegram бот для модели
├── main.py               # Главный файл приложения
├── combine_datasets.py   # Скрипт объединения датасетов
└── requirements.txt      # Зависимости
```

## Добавление датасетов

### Способ 1: Отдельные файлы

1. Поместите файлы в папку `datasets/`
2. Поддерживаемые форматы:
   - `.txt` - текстовые файлы (рекомендуется)
   - `.pdf` - PDF документы
   - `.docx` - Word документы
3. Запустите обучение: `python main.py --train --model-type transformer --model-size small`

### Способ 2: Объединенный датасет (рекомендуется)

Для удобства можно объединить все датасеты в один файл:

```bash
python combine_datasets.py
```

Это создаст файл `datasets/all_combined.txt` со всеми вашими данными.

### Рекомендации по датасетам

- **Минимум**: 10KB текста для Transformer, 1KB для LSTM
- **Оптимально**: 100KB+ для хорошего качества
- **Формат**: обычный текст, UTF-8 кодировка
- **Язык**: любой (русский, английский, смешанный)

## Начальный датасет

Модель инициализирована с информацией о создателях:
- **Создатель**: Jamsaide (Артур)
- **Помощники**: BeeBoo (Никита) и Araxium (Лев)

## Архитектура моделей

### LSTM модель (старая)

- **Тип**: Character-level LSTM
- **Embedding размерность**: 128
- **Hidden размерность**: 256
- **Количество слоев**: 2
- **Токенизация**: Посимвольная (~100-200 символов)
- **Оптимизатор**: Adam
- **Loss функция**: CrossEntropyLoss
- **Контекст**: Ограниченный (забывает длинные зависимости)

### Transformer модель (новая, рекомендуется)

#### Small (рекомендуется для начала) ⭐

- **Embedding размерность**: 128
- **Количество голов внимания**: 4
- **Количество слоев**: 4
- **FFN размерность**: 512
- **Максимальная длина**: 256 токенов
- **Токенизация**: BPE (vocab_size=5000)
- **Dropout**: 0.1
- **Параметры**: ~2M

#### Medium (баланс качества и скорости)

- **Embedding размерность**: 256
- **Количество голов внимания**: 8
- **Количество слоев**: 6
- **FFN размерность**: 1024
- **Максимальная длина**: 512 токенов
- **Токенизация**: BPE (vocab_size=5000)
- **Dropout**: 0.1
- **Параметры**: ~15M

#### Large (максимальное качество)

- **Embedding размерность**: 512
- **Количество голов внимания**: 16
- **Количество слоев**: 12
- **FFN размерность**: 2048
- **Максимальная длина**: 1024 токенов
- **Токенизация**: BPE (vocab_size=5000)
- **Dropout**: 0.1
- **Параметры**: ~100M

### Особенности Transformer

- **Multi-Head Self-Attention**: Модель "смотрит" на текст с нескольких точек зрения одновременно
- **Positional Encoding**: Сохраняет информацию о позиции токенов (sin/cos функции)
- **Causal Masking**: Предотвращает "подглядывание" в будущее при обучении
- **Layer Normalization**: Стабилизирует обучение
- **Residual Connections**: Улучшает градиентный поток через глубокие слои
- **Memory-Efficient Attention**: Оптимизация для длинных последовательностей (>512 токенов)
- **Gradient Clipping**: Предотвращает взрывающиеся градиенты

### BPE Токенизация

Вместо посимвольной обработки, Transformer использует BPE (Byte Pair Encoding):

**Пример:**
- Старый способ (LSTM): "привет" = [п, р, и, в, е, т] (6 токенов)
- Новый способ (Transformer): "привет" = [при, вет] (2 токена)

**Преимущества:**
- Модель учится быстрее
- Понимает морфологию и структуру слов
- Меньше токенов = больше контекста
- Словарь 5000 токенов покрывает большинство слов и их частей

### Стратегии генерации текста

Transformer поддерживает несколько стратегий генерации:

1. **Greedy Decoding** - выбор наиболее вероятного токена (детерминированно)
2. **Top-K Sampling** - выбор из K наиболее вероятных токенов (k=50)
3. **Nucleus (Top-P) Sampling** - выбор из токенов с суммарной вероятностью P (p=0.9)
4. **Temperature Scaling** - контроль случайности:
   - `temperature=0.7` - более предсказуемо
   - `temperature=1.0` - сбалансировано (по умолчанию)
   - `temperature=1.5` - более креативно
5. **Repetition Penalty** - уменьшение повторений (penalty=1.2)

## Тестирование

Проект включает 54+ property-based тестов с использованием Hypothesis:

```bash
# Запуск всех тестов (занимает 15-20 минут)
pytest tests/ -v

# Запуск unit тестов (быстро)
pytest tests/unit/ -v

# Запуск property-based тестов (долго)
pytest tests/property/ -v

# Запуск интеграционных тестов
pytest tests/integration/ -v
```

**Примечание**: Property-based тесты запускают 100+ итераций для каждого свойства, поэтому выполнение занимает время.

## Примеры использования

### Обучение на своих данных

#### Transformer (рекомендуется)

1. Создайте файлы в `datasets/` с вашим текстом (рекомендуется >10KB)
2. Объедините датасеты (опционально):
   ```bash
   python combine_datasets.py
   ```
3. Запустите обучение:
   ```bash
   python main.py --train --model-type transformer --model-size small --epochs 500 --batch-size 64 --lr 0.0005
   ```
4. Начните чат:
   ```bash
   python main.py --model-type transformer --model-size small
   ```

#### LSTM (для сравнения)

1. Создайте файл `datasets/my_data.txt` с вашим текстом
2. Запустите обучение:
   ```bash
   python main.py --train --model-type lstm --epochs 100 --lr 0.001
   ```
3. Начните чат:
   ```bash
   python main.py --model-type lstm
   ```

### Дообучение модели

Обе модели поддерживают инкрементальное обучение:

1. Добавьте новые файлы в `datasets/`
2. Запустите обучение снова с **меньшим learning rate**:

```bash
# Transformer (используйте меньший lr!)
python main.py --train --model-type transformer --model-size small --epochs 50 --lr 0.00005

# LSTM
python main.py --train --model-type lstm --epochs 30 --lr 0.0001
```

**Важно**: Используйте меньший learning rate (в 10 раз меньше) для дообучения, чтобы не забыть предыдущие знания!

### Сравнение моделей

Вы можете обучить обе модели и сравнить их:

```bash
# Обучить LSTM
python main.py --train --model-type lstm --epochs 100

# Обучить Transformer
python main.py --train --model-type transformer --model-size small --epochs 100

# Чат с LSTM
python main.py --model-type lstm

# Чат с Transformer
python main.py --model-type transformer --model-size small
```

### Оптимизация производительности

#### GPU ускорение

Transformer автоматически использует GPU если доступен:

```bash
# Mixed precision training (в 2 раза быстрее на GPU)
python main.py --train --model-type transformer --model-size medium --epochs 100 --mixed-precision --batch-size 64
```

#### CPU режим

Для CPU рекомендуется использовать small модель с меньшим batch-size:

```bash
python main.py --train --model-type transformer --model-size small --epochs 100 --batch-size 16 --lr 0.0005
```

## Устранение неполадок

### Модель не обучается

- Убедитесь, что в `datasets/` есть файлы
- Проверьте, что файлы содержат достаточно текста:
  - LSTM: минимум 100 символов
  - Transformer: рекомендуется >10KB для хорошего качества
- Попробуйте увеличить количество эпох (500+ для Transformer)
- Проверьте, что learning rate не слишком большой (0.0005 для Transformer)

### Программа зависла на "Building BPE vocabulary"

Это нормально! Построение BPE токенизатора занимает 2-5 минут при первом запуске. Подождите, скоро появятся прогресс-бары.

### Ошибка при загрузке PDF

- Установите PyPDF2: `pip install PyPDF2`
- Убедитесь, что PDF не поврежден
- Попробуйте конвертировать PDF в .txt

### Модель генерирует странный текст

#### LSTM
- Обучите модель дольше (100+ эпох)
- Добавьте больше данных для обучения
- LSTM имеет ограниченные возможности - рассмотрите переход на Transformer

#### Transformer
- Убедитесь, что обучили достаточно эпох (минимум 50-100, рекомендуется 500)
- Добавьте больше разнообразных данных (>50KB)
- Попробуйте увеличить model-size (small → medium)
- Уменьшите learning rate (0.0005 → 0.0003)
- Настройте параметры генерации (temperature, top_k, top_p)

### Out of Memory (OOM)

Если не хватает памяти при обучении Transformer:

1. Уменьшите batch-size:
   ```bash
   python main.py --train --model-type transformer --batch-size 16
   ```

2. Используйте меньшую модель:
   ```bash
   python main.py --train --model-type transformer --model-size small
   ```

3. Для CPU используйте LSTM:
   ```bash
   python main.py --train --model-type lstm --batch-size 16
   ```

### Медленное обучение

- Используйте GPU если доступен (автоматически)
- Включите mixed precision: `--mixed-precision` (только GPU)
- Уменьшите model-size (medium → small)
- Уменьшите batch-size (64 → 32)
- Для быстрых экспериментов используйте LSTM

### Loss не уменьшается

- Уменьшите learning rate (0.0005 → 0.0001)
- Увеличьте количество эпох
- Проверьте качество данных (достаточно ли текста?)
- Попробуйте другой размер модели

## Производительность

### Сравнение LSTM vs Transformer

| Метрика | LSTM | Transformer Small | Transformer Medium |
|---------|------|-------------------|-------------------|
| **Скорость обучения** | Быстро | Средне | Медленно |
| **Качество генерации** | Базовое | Хорошее | Отличное |
| **Память модели** | ~10MB | ~50MB | ~200MB |
| **Контекст** | Ограничен | 256 токенов | 512 токенов |
| **Токенизация** | Посимвольная | BPE (5000) | BPE (5000) |
| **Понимание слов** | Нет | Да | Да |
| **История диалога** | Нет | 5 сообщений | 5 сообщений |
| **Параметры** | ~500K | ~2M | ~15M |

### Время обучения (примерно)

**На CPU (Intel i5/i7):**
- LSTM: ~5-10 мин на 100 эпох
- Transformer Small: ~30-60 мин на 100 эпох
- Transformer Medium: ~2-4 часа на 100 эпох

**На GPU (NVIDIA GTX 1060+):**
- LSTM: ~2-5 мин на 100 эпох
- Transformer Small: ~10-20 мин на 100 эпох
- Transformer Medium: ~30-60 мин на 100 эпох

### Рекомендации

- **Для быстрых экспериментов**: LSTM
- **Для качественной генерации**: Transformer Small (⭐ рекомендуется)
- **Для production**: Transformer Medium с GPU
- **Для CPU**: Transformer Small с batch-size=16
- **Для мощного GPU**: Transformer Large с mixed-precision

## Telegram Bot

Проект включает готовый Telegram бот для взаимодействия с моделью. См. `telegram_bot/README.md` для инструкций.

## Документация

- `README.md` - этот файл
- `MIGRATION_GUIDE.md` - руководство по миграции с LSTM на Transformer
- `IMPLEMENTATION_SUMMARY.md` - детали реализации
- `START_AI_MODEL.md` - быстрый старт

## Лицензия

Этот проект создан для образовательных целей.

## Авторы

- **Создатель**: Jamsaide (Артур)
- **Помощники**: BeeBoo (Никита), Araxium (Лев)

---

**Версия**: 2.0 (Transformer Upgrade)  
**Дата обновления**: Февраль 2026  
**GitHub**: https://github.com/Artur566754/BeeJa-AI
