# 🤖 Инструкция по запуску AI Telegram бота

## 📋 Что нужно сделать

### 1. Загрузить файлы на сервер

Загрузи эти файлы/папки на сервер в одну директорию:

```
проект/
├── telegram_bot/
│   ├── bot.py
│   ├── Dockerfile
│   └── docker-compose.yml
├── src/
│   ├── model.py
│   ├── dataset_loader.py
│   ├── chat_interface.py
│   ├── vocabulary.py
│   ├── config.py
│   └── training_pipeline.py
├── main.py
├── datasets/          # Папка с текстовыми файлами для обучения
└── models/            # Папка для сохранения обученной модели
```

### 2. Собрать и запустить через Portainer

**Вариант A: Через Portainer Stacks (рекомендуется)**

1. Зайди в Portainer
2. Перейди в **Stacks** → **Add stack**
3. Выбери **Upload** и загрузи `docker-compose.yml`
4. Или скопируй содержимое `docker-compose.yml` в Web editor
5. Нажми **Deploy the stack**

**Вариант B: Через SSH на сервере**

```bash
cd /путь/к/проекту/telegram_bot
docker-compose build
docker-compose up -d
docker-compose logs -f
```

### 3. Как использовать бота

#### Для администратора (твой Telegram ID: 843366380):

1. **Запусти бота** - отправь `/start`
2. **Обучи модель** - нажми кнопку "🎓 Обучить модель"
   - Введи количество эпох (рекомендуется 50-100 для первого обучения)
   - Подожди завершения обучения
3. **Общайся с AI** - нажми "💬 Чат с AI" и пиши сообщения
4. **Проверь статус** - нажми "📊 Статус модели"
5. **Просмотри датасеты** - нажми "📁 Список датасетов"

#### Для обычных пользователей:

1. Отправь `/start`
2. Нажми "💬 Чат с AI"
3. Общайся с ботом

## 📁 Как добавить данные для обучения

1. Положи текстовые файлы (.txt) в папку `datasets/`
2. Файлы должны содержать диалоги или текст для обучения
3. Запусти обучение через бота

## 🔧 Устранение проблем

### Бот не отвечает
- Проверь логи: `docker-compose logs -f`
- Убедись, что контейнер запущен: `docker ps`

### Модель не обучается
- Проверь, что папка `datasets/` содержит файлы
- Проверь логи обучения в контейнере

### Ошибка "Модель не загружена"
- Сначала обучи модель через кнопку "🎓 Обучить модель"
- После обучения модель сохранится в `/app/models/ai_model.pth`

## 📝 Команды Docker

```bash
# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Пересборка после изменений
docker-compose build --no-cache
docker-compose up -d
```

## ✅ Готово!

Теперь твой AI бот работает и готов к обучению и общению! 🎉
