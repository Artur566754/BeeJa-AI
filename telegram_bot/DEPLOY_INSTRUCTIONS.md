# 🚀 Инструкция по развертыванию AI Telegram бота на сервере

## 📋 Что нужно загрузить на сервер

### 1. **Структура папок на сервере:**

```
/home/your_user/ai-telegram-bot/
├── telegram_bot/
│   ├── bot.py                    # Главный файл бота
│   ├── requirements.txt          # Зависимости Python
│   ├── Dockerfile               # Конфигурация Docker
│   ├── docker-compose.yml       # Конфигурация Docker Compose
│   ├── datasets/                # Папка для датасетов (создастся автоматически)
│   ├── models/                  # Папка для моделей (создастся автоматически)
│   └── bot_data/                # Данные бота (создастся автоматически)
├── src/                         # Исходный код AI модели
│   ├── __init__.py
│   ├── model.py
│   ├── dataset_loader.py
│   ├── training_pipeline.py
│   ├── chat_interface.py
│   ├── vocabulary.py
│   └── config.py
└── main.py                      # Главный файл AI модели
```

## 🔧 Шаг 1: Подготовка файлов

### На своем компьютере:

1. **Создай архив с необходимыми файлами:**

```bash
# Перейди в папку проекта
cd "C:\Users\artur\Documents\Коллелж\video camera"

# Создай архив (можно через WinRAR или 7-Zip)
# Включи в архив:
# - папку telegram_bot/
# - папку src/
# - файл main.py
# - папку datasets/ (с твоими книгами)
```

2. **Или используй Git (если есть):**

```bash
git init
git add telegram_bot/ src/ main.py datasets/
git commit -m "Initial commit"
git push origin main
```

## 🌐 Шаг 2: Загрузка на сервер

### Вариант A: Через FTP/SFTP (FileZilla, WinSCP)

1. Подключись к серверу через FTP/SFTP
2. Загрузи все файлы в папку `/home/your_user/ai-telegram-bot/`

### Вариант B: Через SSH

```bash
# На своем компьютере
scp -r telegram_bot/ src/ main.py datasets/ user@your-server:/home/your_user/ai-telegram-bot/
```

### Вариант C: Через Git (на сервере)

```bash
# На сервере
cd /home/your_user
git clone your-repo-url ai-telegram-bot
cd ai-telegram-bot
```

## 🐳 Шаг 3: Запуск на сервере

### 1. Подключись к серверу через SSH:

```bash
ssh user@your-server-ip
```

### 2. Перейди в папку проекта:

```bash
cd /home/your_user/ai-telegram-bot/telegram_bot
```

### 3. Создай необходимые папки:

```bash
mkdir -p datasets models bot_data
```

### 4. Скопируй датасеты (если еще не скопированы):

```bash
# Если датасеты в родительской папке
cp -r ../datasets/* ./datasets/
```

### 5. Запусти Docker контейнер:

```bash
docker-compose up -d
```

### 6. Проверь логи:

```bash
docker-compose logs -f
```

Должно появиться: `🤖 Бот запущен!`

## ✅ Шаг 4: Проверка работы

1. Открой Telegram
2. Найди своего бота
3. Отправь `/start`
4. Ты должен увидеть админскую панель с кнопками

## 🎓 Шаг 5: Первое обучение модели

1. В боте нажми **"🎓 Обучить модель"**
2. Введи количество эпох (например, `50`)
3. Дождись завершения обучения
4. Бот сообщит, когда обучение закончится

## 📊 Управление ботом на сервере

### Просмотр логов:

```bash
docker-compose logs -f
```

### Остановка бота:

```bash
docker-compose down
```

### Перезапуск бота:

```bash
docker-compose restart
```

### Обновление кода:

```bash
# Останови бота
docker-compose down

# Обнови файлы (через git pull или загрузи новые)
git pull  # или загрузи через FTP

# Пересобери и запусти
docker-compose up -d --build
```

## 📁 Добавление новых датасетов

### Вариант 1: Через FTP/SFTP

1. Подключись к серверу
2. Загрузи файлы в `/home/your_user/ai-telegram-bot/telegram_bot/datasets/`
3. В боте нажми "🎓 Обучить модель"

### Вариант 2: Через SSH

```bash
# На сервере
cd /home/your_user/ai-telegram-bot/telegram_bot/datasets
nano new_data.txt  # Создай и отредактируй файл
```

## 🔒 Безопасность

### ⚠️ ВАЖНО: Не публикуй токен бота!

Если токен попал в публичный репозиторий:

1. Зайди в @BotFather
2. Отправь `/mybots`
3. Выбери своего бота
4. Нажми "API Token"
5. Нажми "Revoke current token"
6. Получи новый токен
7. Обнови `docker-compose.yml`

## 🐛 Решение проблем

### Бот не отвечает:

```bash
# Проверь, запущен ли контейнер
docker ps

# Проверь логи
docker-compose logs -f

# Перезапусти
docker-compose restart
```

### Ошибка при обучении:

```bash
# Проверь, есть ли датасеты
ls -la datasets/

# Проверь логи обучения
docker-compose logs -f | grep training
```

### Модель не загружается:

```bash
# Проверь, существует ли файл модели
ls -la models/

# Если нет - нужно обучить модель через бота
```

## 📝 Полезные команды

### Проверка использования ресурсов:

```bash
docker stats ai-telegram-bot
```

### Очистка старых образов:

```bash
docker system prune -a
```

### Бэкап модели:

```bash
cp models/ai_model.pth models/ai_model_backup_$(date +%Y%m%d).pth
```

## 🎯 Итоговый чеклист

- [ ] Файлы загружены на сервер
- [ ] Docker и Docker Compose установлены
- [ ] Папки datasets/, models/, bot_data/ созданы
- [ ] Датасеты скопированы в datasets/
- [ ] docker-compose.yml настроен с правильным токеном
- [ ] Бот запущен: `docker-compose up -d`
- [ ] Бот отвечает в Telegram
- [ ] Модель обучена через бота
- [ ] Чат с AI работает

## 🆘 Поддержка

Если что-то не работает:

1. Проверь логи: `docker-compose logs -f`
2. Проверь, что все файлы на месте
3. Убедись, что токен бота правильный
4. Проверь, что порты не заняты

---

**Готово!** Теперь твой AI бот работает на сервере 24/7! 🎉
